import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Dashboard - Newsletter App",
};

export default function Dashboard() {
    return (
        <h2>Dashboard</h2>
    );
}